apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bkdata-zookeeper.fullname" . }}-test-connection"
  labels:
    {{- include "bkdata-zookeeper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bkdata-zookeeper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
